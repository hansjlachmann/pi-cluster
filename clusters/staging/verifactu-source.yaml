apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: verifactu
  namespace: flux-system
spec:
  interval: 5m
  url: ssh://git@github.com/erpdevelopmentno/verifactu
  ref:
    branch: main
  secretRef:
    name: verifactu
